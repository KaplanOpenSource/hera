# Workflow editor — checklist

## Catalog-driven form fidelity
- [ ] Default param values on prefill (from the `node_lookup` func)
- [ ] Typed param fields — number / bool / path / dropdown (from the `node_lookup` func)

## Dataflow polish
- [ ] Input handle sits inside the node, not on the left edge
- [ ] Scan nested objects / lists for references (only top-level strings today)
